extends layout.pug

block content
    h1 Authenticate
    
    h2 Reddit
    form(action="/auth/reddit" method="get")
      section
        label(for="username") FireStore Username:
        input.w500(type="text" name="username")
      section
        button(type="submit") Submit
